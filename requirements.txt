# Virtual Band AI - Dependências (Compatíveis com Windows e Python 3.14)

# --- Núcleo obrigatório (DSP / MIR / Áudio) ---
numpy>=1.26.0
scipy>=1.12.0
soundfile>=0.12.0
sounddevice>=0.5.0
librosa>=0.10.0

# --- Importação de cifras (opcional, por formato) ---
# Sem estes pacotes, o núcleo de áudio funciona normalmente; apenas a importação
# do formato correspondente fica indisponível (o app avisa e ignora o formato).
Pillow>=10.0.0        # Imagens e reconstrução espacial 2D de cifras
python-docx>=1.1.0    # Importação de cifras em .docx
pypdf>=4.0.0          # Extração de texto de PDFs digitais
pytesseract>=0.3.10   # OCR de cifras escaneadas (requer Tesseract instalado no sistema)

# --- Apenas para a suíte de testes ---
reportlab>=4.0.0      # Geração de PDFs de teste
